# Requirements for the dGC documentation

# Zensical theme
zensical==0.0.59

# for the feature which displays authors at the bottom of the page
# NOTE FOR FUTURE (added 2026.03.05): Zensical does not support this plugin
# However they are planning to support it, when the module system is built
# Issue: https://github.com/zensical/backlog/issues/17
# Re-enable when the feature is added
# mkdocs-git-committers-plugin-2

# Jinja2 templating in Markdown (e.g. {{ repository_name }} badges in products/*.md)
# Now built into Zensical as the `zensical.extensions.macros` markdown extension
# (shipped in Zensical 0.0.40): https://github.com/zensical/backlog/issues/16
# No separate package required - enabled in mkdocs.yml under markdown_extensions.

# for the PDF export feature
# NOTE FOR FUTURE (added 2026.03.05): Zensical does not support this plugin
# However they are planning PDF export support, when the module system is built
# Follow this issue: https://github.com/zensical/backlog/issues/25
# Re-enable when the feature is added
# mkdocs-with-pdf

# for the timestamp feature
# NOTE FOR FUTURE (added 2026.03.05): Zensical does not support this plugin
# However they are planning to support it, when the module system is built
# Follow this issue: https://github.com/zensical/backlog/issues/18
# Re-enable when the feature is added
# mkdocs-git-revision-date-localized-plugin

# LLMs.txt generation
# NOTE FOR FUTURE (added 2026.03.05): Zensical does not support this plugin
# However there is an Issue for a similar feature
# Follow this issue https://github.com/zensical/backlog/issues/92
# Re-enable when the feature is added
# mkdocs-llmstxt

# spelling / common typo checks for docs
codespell==2.4.3

# markdown linting
pymarkdownlnt==0.9.39

# self-hosted PDF export of the Clinical Safety and Medical Device sections
# (see pdf-export/build-safety-pdf.py and s/build-pdf). Replaces the suspended
# mkdocs-with-pdf plugin for the safety documentation only. WeasyPrint's native
# libraries (libpango etc.) are already installed in the Dockerfile.
weasyprint==69.0
beautifulsoup4==4.15.0

# navigation completeness checks
PyYAML==6.0.3

# dependency vulnerability auditing
pip-audit==2.10.1
