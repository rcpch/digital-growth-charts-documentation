#!/usr/bin/env bash
#
# Serves the documentation locally through the repository's Docker Compose
# environment. `s/up` remains available as the Compose-oriented alias.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

exec "$REPO_ROOT/s/up" "$@"
